# Handoff — 2026-08-11 → 08-12 marathon session (workspace 2)

## State

A day-long incident-and-hardening run across DR3-Vision + the fleet. Everything below is MERGED, DEPLOYED to CHAD, and verified in the running container unless marked otherwise. All four required checks green on every PR; nothing force-merged (one early exception self-reported and corrected: #245 merged on pending checks, later verified green).

### DR3-Vision shipped (PRs #228–#254)
- **#228 / ADR-0091** — consumed-slot cards: shared `describeConsumedSlot`, resume/takeover links on both Hauls+Queue, chokepoint parity test. (Costco Innovel / Pablo incident, Mon AM.)
- **#229/#231–#233 / ADR-0092** — stale-claim watchdog, LIVE both sites (silence-based detection via GREATEST(parent, stacks, photos); badge 2h, manager email 4h, 1 email/site/day cap, no auto-release). Pilot→live flipped 8/11 12:50 PM PT; first real delivery Morena+Janette 12:51 PM. Route-401 class fixed via public-paths sweep test (also found+fixed dead `/api/internal/idempotency/sweep`).
- **#230 / ADR-0019.3** — Patrick Dills = Eugene ops signer (replaced Shannon's 8/8 cover; Kelsey already out 8/8). Auto-override actor repointed to active bill.barnard@svdp.us at BOTH sites; seed fixed so re-seeds can't re-kill it (root cause of the July 7 recurrence) + seed no longer names Kelsey in chains.
- **#234** — bonus chain-health pill: `/admin/bonus-chain-health`, subsystem entry, 06:30 PT daily cron, `high` on transition only, ledger records `paged=false` on drops. Both sites green. Verified auto-sign WILL fire Aug 18 (all 4 escalation tiers proven firing on Aug 4; t3 refused correctly on dead actor; T-213 e2e test 4/4).
- **#235/#236 / ADR-0019.5** — em-dash→ByteString ntfy header bug: `toHeaderSafe()` at both publishers' choke points; every MyMRC page had been dropping since ship. Sweep test bars unsanitized X-Title.
- **#238 / ADR-0095** — doc-ingest paging ledger stamped before publish (dropped page suppressed its own 24h retry); publish-then-stamp now. + healed download_failed held open on applied-path only.
- **#241 / ADR-0096** — late-truck reconcile: `reconcilableExpectedLoadId` state + two-tap day acknowledgement; server-side Pacific day guard added to `startInboundLoad` (was UI-only); D-8 silent write-refusals now speak via `classifyWriteRefusal` on all 4 clients. (H-136980 Speedy Delivery incident — crew completed same evening.)
- **#242/#245/#247 (Terry)** — ADR-0094 "why the floor keeps calling" landed (48.3% slot divergence; missing-states frame; floor system 10 operating days old); ADR-0065 Am.2 written (real UTC date-default bug from 7/30); ADR citation CI gate (190ms, ratcheting baseline, duplicate-number detection after the 0097 collision); `docs/adr/PROMISES.md` (33 rows). ADR-0098 records design.
- **#244 / ADR-0097** — alert tunings: sweep_failed pages on 2nd consecutive failure only (data: 689 sweeps/7d, 0.58% fail, none consecutive); subscription_renew demoted per-occurrence to dashboard. TEREX staged backlog cleared (verified byte-identical vs live file; 5 audit rows; staged=0). KNOWN OPEN: extracted aggregates are exactly 2× real (parser counts sheet totals rows) — needs own change + re-baseline, deliberately not hot-fixed.
- **#246 / ADR-0019.3 §2 → RESOLVED** — Patrick SoD guard: (person, period) exclusion incl. facility-override leak test; verified SOD-LIVE in container. Residuals: `canApproveRequest` untouched (Bill's call); guard depends on bonus_employees.user_id link staying populated.
- **#243** — H-135793 attribution doc (`docs/INVENTORY-SOURCE-ATTRIBUTION.md`): WOODLAND (not Eugene), 150/150 program, 8:00 AM docking, "Humboldt Sanitation" authoritative vs email's "Humble Moving"; load↔haul link is 4-check inference, not FK.
- **#248/#251/#252 / ADR-0099, ADR-0100** — dead-end slice: 16 findings closed (void-screen, floor not-found.tsx ×3 locales, status-map unification, blocked-photos surface, untranslated-string tests); scrape cancel now needs 3 consecutive misses (67 of 69 historical cancels were flaps that self-restored) + empty-pass wipe fenced + withdrawn slots visible; dead-end telemetry live on /metrics. Ship-before-noon rule in CONTRIBUTING.md.
- **#249 / ADR-0071 follow-through** — processor performance monitor: was NEVER broken — `enabled=false` since ship 7/31 pending Bill's quota decision, and nothing could say so. Now: heartbeat row every run, read-only evaluation of disabled sites, amber/red health pill, Eugene config seeded (disabled, recipients empty). Woodland median 64/day vs quota 75 → at 75/2 the digest names 18 of 21 processors. NOT ENABLED — Bill's switch.
- **#254 (deploy pending at handoff time)** — Woodland daily-log workbook sync: `drive_upn` was a SharePoint URL fragment (`kelsey_ruhland@` vs `kelsey.ruhland@`) AND transport `$select` omitted the `file` facet → listFolder returned zero files for every folder everywhere (1,098 polls, 0 reads since 7/31; the file existed all along, floor writing nightly). + monthly folder-path templating (would have re-broken Sept 1; folders exist through Dec). Registration fixed live w/ audit row. WATCH: confirm first successful read + row count after container swap.

### Fleet (noc-master + cross-repo)
- **ADR-0200 + Am.1–4** — fleet ntfy header-safety contract: 24 conformance vectors (canonical `data/ntfy-header-conformance.json`), per-language canonical impls, vendored+pinned. 17 implementations / 14 repos all 24/24, measured from origin/main. Both "already fixed" impls (noc Node ADR-0063, Python) had 6 divergences each incl. CRLF-injection hole. httpx is ASCII-strict → accent-folding contract. barnardhq Worker: ~2.5 MONTHS of lead notifications never sent (emoji in header); fixed, deployed 8/11 10:10 PM, but `worker/` has NO automated deploy path (ADR-0014 in barnardhq repo) — manual wrangler step recurs.
- **ADR-0201 + Am.1–2 (noc-master)** — deployer verifies the `up` (created = failure); rollback env-file path bug fixed (rollback matrix was fiction for TitanForge/KEYMASTER); self-update drain bounded 10min.
- **ADR-0202** — deploy-record honesty: 151 deferral rows fleet-wide had been recorded `success`; deferred stays deferred + `deferral_resolved`. `pin_built_digest` only matters for the 2 swarm repos; ClaudeSync on pinned path (stale-July-image drift rolls on next deploy).
- **ADR-0203 + Am.1–3** — KEYMASTER onboarded (Pattern A, `version_file: rotator/VERSION`); clean-verify deploy succeeded; expected-containers check now label-based (both consumers one contract); bump path honors version_file (KEYMASTER's version_assert could never fail before); cached-rebuild digest finding (provenance/SBOM attestations cause fresh digests even fully cached; falsified with flags off).
- **ADR-0204** — agents-sharing-a-machine conventions (memory-index merge, worktree-per-agent, additive CHANGELOG, report routing). Written after 4 real collisions in one night.
- **marketing** — leaked ntfy token was ALREADY dead (7/16 rotation); scripts now read secrets from env (10 occurrences / 8 files redacted on main); second dead token found in tier1b live config (repointed+verified). **Zoho app password `bill@barnardhq.io` STILL VALID in git history — Bill must rotate at Zoho** (app-specific, safe to rotate). History rewrite deferred until after rotation; commands in `marketing/docs/audits/2026-08-11-tier1b-secret-exposure-remediation.md` §7.
- Workspace-3 session (cloudflared repairs) shipped TitanForge ADR-0083 DEPLOYER_SERVICES selective-build + saturation honesty; shared-tree commits coordinated cleanly. NOTE: "ADR-0083" is now ambiguous between repos — cite as "TitanForge's ADR-0083".

## Next / open decisions (Bill)
1. **Zoho password rotation** (only live leaked secret; then optional history rewrite).
2. **Processor quota settings**: quota + min_misses (data suggests ~50/3 → ~2.8 names/wk vs 75/2 → 18 of 21) and whether Eugene alerts + recipients. Settings only, no deploy. Then flip `enabled`.
3. **Discovery gap**: which of the 8 readable-but-unregistered SharePoint docs should be watched.
4. **TEREX 2× parser fix + re-baseline** (extraction counts totals rows; needs own change).
5. **Stale-claim watchdog**: watch first week; a false-positive report from Morena/Janette is the most valuable signal.
6. Smaller: `canApproveRequest` SoD gap; withdrawn-slot restore (P-34, billing artifact); 5 phantom ADR amendments (P-01…P-05) need their original authors; noc-master DECISIONS index gap 0196–0203; emoji-in-labels at barnardhq Worker source; drive-404 vs folder-404 disambiguation + driveUpn validation; `processor_quota_config.send_dow/send_time_pt` stored-but-unread.
7. **Aug 18 07:00–09:00 PT**: Patrick's first live bonus signing (Eugene P17, pay date 8/21). Auto-override verified working; health pill watching; he gets exactly ONE email (no reminders).

## Context / gotchas for the next session
- CHAD deployer defers under ADR-0062 saturation (agent test suites trip it); ALWAYS verify the container, not git HEAD (build-races-pull class).
- DR3 shared checkout `/home/bbarnard065/DR3-Vision` had heavy multi-agent traffic; ~16 worktrees, most stale — cleanup candidate. An untracked now-redundant ADR-0094 copy sits there.
- ntfy 200 = acceptance, not delivery; Graph 202 same. Honest-ledger pattern (record delivered, not attempted) is now the repo standard — follow it in new alert code.
- Test doubles more permissive than reality caused ≥3 of this week's escapes (fetch-header mock, $select-less Graph mock, where-ignoring findMany double). When touching transports, ask what the mock can't see.
- Floor system is 10 operating days old; 48.3% of slots diverge from happy path; expect ADR-0100 telemetry to look noisy its first week — that's the instrument working.
- Eugene site has no floor load traffic yet; watchdog + quota configs seeded there deliberately dark.
